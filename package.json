{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"⚠️ No hay pruebas definidas todavía\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}
